package ca.pfaj.minimagic;

/*
 * A class-based implementation of spells:
 * - each current spell is turned into an implementation of SpellProperties
 * - to create a spell, the Main method passes the SpellProperties implementation into a Spell constructor
 */

public class Spell
{
	public Spell(SpellProperties properties)
	{
		/*
		 * TODO:
		 * register ingredients
		 * register repeating methods
		 * register click events
		 * register init and disable
		 */
	}
}
